//------------------------------------------------------------//
// ./js/app.js
// Create : 2025/06/13
// Obniz制御
//------------------------------------------------------------//

'use strict';



/*function a( ){
  obniz.onconnect = async () => {
    temp_sensor = obniz.wired( "LM35DZ", { gnd: 0, output: 1, vss: 2 });
    await obniz.wait( 100 );
    temp_sensor.onchange(( temps ) => {
      temp = temps;
    })
  }
}

async function b( ){
  return temp;
}

const get_temp = async ( ) => {
  return await new Promise(( resolve ) => {
    resolve( temp_sensor.getWait( ));
  });
}

module.exports = { a, b };*/